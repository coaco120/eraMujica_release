;==================================================
;
;	绝顶系参照関数置き场
;	
;==================================================
;-------------------------------------------------
;関数名:_SHOOTING
;概　要:射精种から字符串被取得做関数
;引　数:ARG…(TCVAR:射精)
;戻り値:射精种被表す字符串
;備　考:式中関数。内部関数和して的使用被想定
;-------------------------------------------------
@_SHOOTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "无/空射精/射精/大量射精/早泄/前列腺射精", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SHOOTING
;概　要:ARG的射精种被取得做
;;引　数:ARG…角色登录番号。省略做和TARGET
;戻り値:射精种被表す字符串
;備　考:式中関数
;-------------------------------------------------
@GET_SHOOTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:射精)

;-------------------------------------------------
;関数名:SHOOTING
;概　要:ARGS但指定した射精种那么真被返す
;引　数:ARG…角色番号
;引　数:ARGS…射精种
;戻り値:そ的状态那么1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SHOOTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SHOOTING(TCVAR:ARG:射精)

;单純にMILKで良い気但したけどどこかで被りそうな気もした的でSPLAYING!
;-------------------------------------------------
;関数名:_SPLAYING_MILK
;概　要:喷乳种から字符串被取得做関数
;引　数:ARG…(TCVAR:喷乳)
;戻り値:射精种被表す字符串
;備　考:式中関数。内部関数和して的使用被想定
;-------------------------------------------------
@_SPLAYING_MILK(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "无/空喷乳/喷乳/大量喷乳", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SPLAYING_MILK
;概　要:ARG的喷乳种被取得做
;;引　数:ARG…角色登录番号。省略做和TARGET
;戻り値:喷乳种被表す字符串
;備　考:式中関数
;-------------------------------------------------
@GET_SPLAYING_MILK(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:喷乳)

;-------------------------------------------------
;関数名:SPLAYING_MILK
;概　要:ARGS但指定した喷乳种那么真被返す
;引　数:ARG…角色番号
;引　数:ARGS…喷乳种
;戻り値:そ的状态那么1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SPLAYING_MILK(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SPLAYING_MILK(TCVAR:ARG:喷乳)

;FEMALE_EJACULATION和かSHIOFUKI等是論外だ和思われる的でSQUIRTING!
;-------------------------------------------------
;関数名:_SQUIRTING
;概　要:潮吹から字符串被取得做関数
;引　数:ARG…(TCVAR:潮吹)
;戻り値:潮吹种被表す字符串
;備　考:式中関数。内部関数和して的使用被想定
;-------------------------------------------------
@_SQUIRTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "无/潮吹/大量潮吹", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SQUIRTING
;概　要:ARG的潮吹种被取得做
;;引　数:ARG…角色登录番号。省略做和TARGET
;戻り値:潮吹种被表す字符串
;備　考:式中関数
;-------------------------------------------------
@GET_SQUIRTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SQUIRTING(NOWEX:ARG:潮吹)

;-------------------------------------------------
;関数名:SQUIRTING
;概　要:ARGS但指定した潮吹种那么真被返す
;引　数:ARG…角色番号
;引　数:ARGS…潮吹种
;戻り値:そ的状态那么1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SQUIRTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SQUIRTING(TCVAR:ARG:潮吹)

;-------------------------------------------------
;関数名:_DRY_CUM
;概　要:空射精种から字符串被取得做関数
;引　数:ARG…(TCVAR:空射精)
;戻り値:空射精种被表す字符串
;備　考:式中関数。内部関数和して的使用被想定
;-------------------------------------------------
@_DRY_CUM(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "无/空射精/前列腺", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_DRY_CUM
;概　要:ARG的空射精种被取得做
;;引　数:ARG…角色登录番号。省略做和TARGET
;戻り値:空射精种被表す字符串
;備　考:式中関数
;-------------------------------------------------
@GET_DRY_CUM(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _DRY_CUM(TCVAR:ARG:空射精)

;-------------------------------------------------
;関数名:DRY_CUM
;概　要:ARGS但指定した空射精种那么真被返す
;引　数:ARG…角色番号
;引　数:ARGS…空射精种
;戻り値:そ的状态那么1、違えば0
;備　考:式中関数
;-------------------------------------------------
@DRY_CUM(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _DRY_CUM(TCVAR:ARG:空射精)
